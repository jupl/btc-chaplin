'use strict'

{{uppercamelize name}}View = require('views/{{dasherize name}}')

describe '{{uppercamelize name}}View', ->

  beforeEach ->
    @{{camelize name}}View = new {{uppercamelize name}}View

  it '', ->


  afterEach ->
    @{{camelize name}}View.dispose()
